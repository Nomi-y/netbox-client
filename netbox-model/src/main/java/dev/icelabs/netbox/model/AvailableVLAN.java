/*
 * NetBox REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.5.2-Docker-4.0.0 (4.5)
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package dev.icelabs.netbox.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import dev.icelabs.netbox.model.BriefVLANGroup;
import java.io.IOException;
import java.io.Serializable;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.StringJoiner;

/**
 * Representation of a VLAN which does not exist in the database.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.20.0-SNAPSHOT")
public class AvailableVLAN implements Serializable {
  private static final long serialVersionUID = 1L;

  public static final String SERIALIZED_NAME_VID = "vid";
  @SerializedName(SERIALIZED_NAME_VID)
  @javax.annotation.Nonnull
  private Integer vid;

  public static final String SERIALIZED_NAME_GROUP = "group";
  @SerializedName(SERIALIZED_NAME_GROUP)
  @javax.annotation.Nullable
  private BriefVLANGroup group;

  public AvailableVLAN() {
  }
  /**
   * Constructor with only readonly parameters
   */
  
  public AvailableVLAN(
     Integer vid, 
     BriefVLANGroup group
  ) {
    this();
    this.vid = vid;
    this.group = group;
  }

  /**
   * Get vid
   * @return vid
   */
  @javax.annotation.Nonnull

  public Integer getVid() {
    return vid;
  }



  /**
   * Get group
   * @return group
   */
  @javax.annotation.Nullable

  public BriefVLANGroup getGroup() {
    return group;
  }




  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AvailableVLAN availableVLAN = (AvailableVLAN) o;
    return Objects.equals(this.vid, availableVLAN.vid) &&
        Objects.equals(this.group, availableVLAN.group);
  }

  @Override
  public int hashCode() {
    return Objects.hash(vid, group);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AvailableVLAN {\n");
    sb.append("    vid: ").append(toIndentedString(vid)).append("\n");
    sb.append("    group: ").append(toIndentedString(group)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    // add `vid` to the URL query string
    if (getVid() != null) {
      try {
        joiner.add(String.format(java.util.Locale.ROOT, "%svid%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getVid()), "UTF-8").replaceAll("\\+", "%20")));
      } catch (UnsupportedEncodingException e) {
        // Should never happen, UTF-8 is always supported
        throw new RuntimeException(e);
      }
    }

    // add `group` to the URL query string
    if (getGroup() != null) {
      joiner.add(getGroup().toUrlQueryString(prefix + "group" + suffix));
    }

    return joiner.toString();
  }

  public static class Builder {

    private AvailableVLAN instance;

    public Builder() {
      this(new AvailableVLAN());
    }

    protected Builder(AvailableVLAN instance) {
      this.instance = instance;
    }

    public AvailableVLAN.Builder vid(Integer vid) {
      this.instance.vid = vid;
      return this;
    }
    public AvailableVLAN.Builder group(BriefVLANGroup group) {
      this.instance.group = group;
      return this;
    }


    /**
    * returns a built AvailableVLAN instance.
    *
    * The builder is not reusable.
    */
    public AvailableVLAN build() {
      try {
        return this.instance;
      } finally {
        // ensure that this.instance is not reused
        this.instance = null;
      }
    }

    @Override
    public String toString() {
      return getClass() + "=(" + instance + ")";
    }
  }

  /**
  * Create a builder with no initialized field.
  */
  public static AvailableVLAN.Builder builder() {
    return new AvailableVLAN.Builder();
  }

  /**
  * Create a builder with a shallow copy of this instance.
  */
  public AvailableVLAN.Builder toBuilder() {
    return new AvailableVLAN.Builder()
      .vid(getVid())
      .group(getGroup());
  }


}

