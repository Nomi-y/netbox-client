/*
 * NetBox REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.5.2-Docker-4.0.0 (4.5)
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package dev.icelabs.netbox.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import dev.icelabs.netbox.model.BriefVRF;
import java.io.IOException;
import java.io.Serializable;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.StringJoiner;

/**
 * Representation of a prefix which does not exist in the database.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.20.0-SNAPSHOT")
public class AvailablePrefix implements Serializable {
  private static final long serialVersionUID = 1L;

  public static final String SERIALIZED_NAME_FAMILY = "family";
  @SerializedName(SERIALIZED_NAME_FAMILY)
  @javax.annotation.Nonnull
  private Integer family;

  public static final String SERIALIZED_NAME_PREFIX = "prefix";
  @SerializedName(SERIALIZED_NAME_PREFIX)
  @javax.annotation.Nonnull
  private String prefix;

  public static final String SERIALIZED_NAME_VRF = "vrf";
  @SerializedName(SERIALIZED_NAME_VRF)
  @javax.annotation.Nullable
  private BriefVRF vrf;

  public AvailablePrefix() {
  }
  /**
   * Constructor with only readonly parameters
   */
  
  public AvailablePrefix(
     Integer family, 
     String prefix, 
     BriefVRF vrf
  ) {
    this();
    this.family = family;
    this.prefix = prefix;
    this.vrf = vrf;
  }

  /**
   * Get family
   * @return family
   */
  @javax.annotation.Nonnull

  public Integer getFamily() {
    return family;
  }



  /**
   * Get prefix
   * @return prefix
   */
  @javax.annotation.Nonnull

  public String getPrefix() {
    return prefix;
  }



  /**
   * Get vrf
   * @return vrf
   */
  @javax.annotation.Nullable

  public BriefVRF getVrf() {
    return vrf;
  }




  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AvailablePrefix availablePrefix = (AvailablePrefix) o;
    return Objects.equals(this.family, availablePrefix.family) &&
        Objects.equals(this.prefix, availablePrefix.prefix) &&
        Objects.equals(this.vrf, availablePrefix.vrf);
  }

  @Override
  public int hashCode() {
    return Objects.hash(family, prefix, vrf);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AvailablePrefix {\n");
    sb.append("    family: ").append(toIndentedString(family)).append("\n");
    sb.append("    prefix: ").append(toIndentedString(prefix)).append("\n");
    sb.append("    vrf: ").append(toIndentedString(vrf)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    // add `family` to the URL query string
    if (getFamily() != null) {
      try {
        joiner.add(String.format(java.util.Locale.ROOT, "%sfamily%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getFamily()), "UTF-8").replaceAll("\\+", "%20")));
      } catch (UnsupportedEncodingException e) {
        // Should never happen, UTF-8 is always supported
        throw new RuntimeException(e);
      }
    }

    // add `prefix` to the URL query string
    if (getPrefix() != null) {
      try {
        joiner.add(String.format(java.util.Locale.ROOT, "%sprefix%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getPrefix()), "UTF-8").replaceAll("\\+", "%20")));
      } catch (UnsupportedEncodingException e) {
        // Should never happen, UTF-8 is always supported
        throw new RuntimeException(e);
      }
    }

    // add `vrf` to the URL query string
    if (getVrf() != null) {
      joiner.add(getVrf().toUrlQueryString(prefix + "vrf" + suffix));
    }

    return joiner.toString();
  }

  public static class Builder {

    private AvailablePrefix instance;

    public Builder() {
      this(new AvailablePrefix());
    }

    protected Builder(AvailablePrefix instance) {
      this.instance = instance;
    }

    public AvailablePrefix.Builder family(Integer family) {
      this.instance.family = family;
      return this;
    }
    public AvailablePrefix.Builder prefix(String prefix) {
      this.instance.prefix = prefix;
      return this;
    }
    public AvailablePrefix.Builder vrf(BriefVRF vrf) {
      this.instance.vrf = vrf;
      return this;
    }


    /**
    * returns a built AvailablePrefix instance.
    *
    * The builder is not reusable.
    */
    public AvailablePrefix build() {
      try {
        return this.instance;
      } finally {
        // ensure that this.instance is not reused
        this.instance = null;
      }
    }

    @Override
    public String toString() {
      return getClass() + "=(" + instance + ")";
    }
  }

  /**
  * Create a builder with no initialized field.
  */
  public static AvailablePrefix.Builder builder() {
    return new AvailablePrefix.Builder();
  }

  /**
  * Create a builder with a shallow copy of this instance.
  */
  public AvailablePrefix.Builder toBuilder() {
    return new AvailablePrefix.Builder()
      .family(getFamily())
      .prefix(getPrefix())
      .vrf(getVrf());
  }


}

